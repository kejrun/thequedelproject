/*import {
  STATE_FEEDPOST
} from '../actions/types';

const INITIAL_STATE = {
  feedpostState: []
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    case STATE_FEEDPOST:
    console.log(action.payload);
      return action.payload;
    default:
      return state;
  }
};*/
